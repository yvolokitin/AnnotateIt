import 'dart:typed_data';

Future<bool> samInit({String? encoderUrl, String? decoderUrl}) async => false;

Future<Uint8List?> samRun(
  Float32List inputNCHW,
  int origW,
  int origH,
  double tapX1024,
  double tapY1024,
) async => null;
